fastapi==0.109.0
uvicorn[standard]==0.27.0
httpx==0.26.0
websockets==12.0
pydantic==2.5.0
pydantic-settings==2.1.0
python-dotenv==1.0.0
sqlalchemy[asyncio]==2.0.25
aiosqlite==0.19.0
greenlet>=3.0.0

# AI Intelligence Layer
pyyaml>=6.0
markdown>=3.5

# News Intelligence Layer (semantic matching)
numpy>=1.24.0
sentence-transformers>=2.2.0  # Embedding model (optional — TF-IDF fallback if missing)
faiss-cpu>=1.7.0              # Fast vector search (optional — numpy fallback if missing)
