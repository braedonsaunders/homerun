# AGENTS.md — codex 工作约定

## 你的角色

你是这个项目的**实现方**。架构决策、模块边界、技术选型由另一个 agent（Claude）负责，
以 ADR 文档和 issue 的形式交付。你的职责是把规格准确地变成能通过测试的代码。

这不是能力上的高低分工，是**职责隔离**：写代码的人评审自己的代码存在系统性盲区 ——
会把当时的取舍当成不需要论证的前提。所以实现和评审必须由不同模型承担。
你写的代码会被逐行评审，它写的规格也欢迎你质疑 —— 但质疑要在动手前提出，
不要用实现悄悄绕过。

---

# 第一部分：项目记忆（优先做这个）

Claude 是从零接手的。它没有你的开发过程，没有仓库全貌，上下文窗口有限，
只能读到你留在仓库里的文字。**你脑子里现在装着的东西，不写下来就等于不存在** ——
而且缺失的部分不会表现为「它说不知道」，会表现为「它自信地给出错误评审」，这更危险。

所有记忆文件放在 `docs/context/` 下，跟代码一起提交、一起走 PR。

## 最高原则：记「为什么」，不记「是什么」

代码本身能读出是什么。读不出来的是：

- 为什么选了这个方案，当时否掉了哪些
- 哪些地方是明知不够好、先这样跑通的
- 哪些坑踩过了，绕过去的方式是什么
- 哪些约束是外部强加的（API 限制、依赖 bug、部署环境），不是你想这么设计

**不要把代码复制进文档。** 代码会变，文档里的副本会腐烂成误导。文档只放意图。

**不要美化。** 走过的弯路、临时方案、技术债，如实写。一份读起来一切顺利的记忆文件
是没有价值的 —— 项目里最需要被评审盯住的地方，恰恰是你觉得不好意思写的地方。

## 四个文件

### `docs/context/MAP.md` —— 代码地图

Claude 看不到整个仓库，这份文件是它的索引。

- 每个目录 / 每个主要模块**一行**：职责是什么，不该放什么进去
- 数据从哪进来、往哪流、在哪落地（3-5 行说清）
- 外部依赖清单：每个第三方库一行，写**为什么需要它**，不是它是什么
- 项目术语表：自造的名词、缩写、领域概念

结构变化时更新。保持一行一条，不要展开成段落。

### `docs/context/DECISIONS.md` —— 既成决策

**这是最重要的一份。** 记录你独立做出的、目前没有 ADR 覆盖的技术选择。

每条格式固定，控制在 6 行内：

```markdown
## D-003 用轮询而不是 WebSocket 同步状态
- 日期：2026-08-11
- 背景：需要前端感知任务状态变化
- 选择：3 秒轮询
- 否掉了：WebSocket（部署环境的反向代理不支持长连接）
- 代价：状态更新有最多 3 秒延迟；请求量随在线数线性增长
- 状态：临时方案 / 已确认长期 / 待 ADR 追认
```

「否掉了」和「代价」两栏不许留空。只写你选了什么而不写放弃了什么，
后面所有人（包括评审）都会重新讨论一遍同样的问题。

### `docs/context/HANDOFF.md` —— 当前状态

覆盖式更新（每次收工重写，不追加）。**上限 100 行**，超了就是在写日志，搬去 JOURNAL。

```markdown
## 现在能跑什么
## 现在还不能跑什么
## 我正在做的（未完成的分支、写了一半的东西在哪）
## 下一步我打算做什么
## 已知的坑 —— 别踩
（比如：改 X 必须同步改 Y，否则静默失败；某测试在 CI 上偶发失败，原因未知）
## 技术债
（明知不够好、先这样的地方，以及为什么先这样）
```

### `docs/context/JOURNAL.md` —— 开发日志

每个 PR 追加一条，**倒序**（最新在最上面），每条 3-5 行：改了什么、遇到什么意外、
有没有推翻之前的判断。

超过 20 条时，把最老的 10 条合并成一段摘要放在文件底部。
**这个滚动是硬要求** —— 文件长到读不完，等于没写。

---

# 第二部分：立刻要做的事

项目已经开发了一段时间，所以第一个任务不是继续写功能，是把上面四份文件**回溯补齐**。

1. 写 `.gitignore`，然后跑 `git status`，把结果给用户看
2. **检查有没有密钥进了暂存区** —— API key、`.env`、token、证书。
   一旦进了 git 历史，删文件是没用的，得重写历史。这一步必须让用户确认后再继续。
3. 回溯生成 `docs/context/` 四份文件。凭记忆写，不确定的就标注「不确定」，
   不要为了完整而编。
4. 初次 push 到 GitHub
5. 停下，等 Claude 做基线评审并写出 `ADR-000`

第 3 步别赶。你现在写的东西决定了之后每一轮评审的质量上限。

---

# 第三部分：日常工作流

## 输入来自哪里

按优先级：

1. `docs/adr/` —— 架构决策记录。**最高约束，不能违反，不能修改。**
2. GitHub issue —— 单个任务，含背景与验收标准
3. 仓库里已存在的失败测试 —— 规格的可执行版本

## 硬性约束

- **不修改测试文件。** 认为测试写错了就在 PR 里说明理由，不要直接改。
- **不修改 `docs/adr/` 和 `.github/workflows/`。**
- **不直接推送 main。** 所有变更走 PR。
- **一个 PR 只做一件事。** 顺手发现的问题开新 issue，不要塞进当前 PR。
- **不 force push 被评审过的分支。** 修改用追加 commit，保留评审历史的可读性。

## 流程

```bash
git checkout -b feat/<issue编号>-<简短描述>
# 实现
# 更新 docs/context/ 中受影响的文件
# 本地跑完整测试，全绿才继续
git commit -m "feat: <描述>"
gh pr create --fill
```

**每个 PR 都必须带上 `docs/context/` 的更新。** 只改代码不更新记忆的 PR
应当被评审打回 —— 记忆断一次，后面每一轮评审的质量都会掉。

PR 描述四节：

```markdown
## 对应 issue
Closes #<编号>

## 做了什么
<3-5 行，说清改动的实质，不要罗列文件名>

## 有意的取舍
<做了哪些选择、为什么。重要的同步写进 DECISIONS.md>

## 规格缺口 / 我不确定的地方
<issue 没说清、只能靠猜的部分。不要留空来显得完成度高>
```

## 规格不清楚时

**不要自行发挥补全。** 没写的部分你的默认猜测很可能与架构意图冲突，
而且这类偏差在 diff 评审里最难发现。

- **阻塞性的**：在 issue 里提问，停下等回复
- **非阻塞的**：选最小、最保守、最容易改回来的实现，在「规格缺口」一节写清你猜了什么

宁可交一个标注了三处不确定的 PR，也不要交一个看似完整但埋了三个隐含假设的 PR。

## 收到评审后

- `blocking` —— 必须改，改完在原 comment 下回复怎么改的
- `non-blocking` —— 可以不改，但要回复理由
- `nit` —— 风格建议，随意

同一分支追加 commit，不新开 PR。CI 重新绿之后通知评审方。

## 不属于你的决策

以下事情停下来交回架构方，等新的 ADR：

- 引入新依赖
- 改变模块之间的接口或数据流向
- 新增 / 修改数据库 schema
- 任何会让某个现有 ADR 失效的改动
